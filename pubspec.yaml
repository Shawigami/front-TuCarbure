name: tucarbure
description: 3 Projet Licence
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  url_launcher: ^6.0.12
  android_intent_plus: ^2.0.2
  http: ^0.13.2
  lehttp_overrides: ^1.0.3
  latlong2: ^0.8.0
  intl: ^0.17.0
  geolocator: ^7.6.2
  flutter_map: ^0.13.1  # Ajout de la dépendance flutter_map
  cupertino_icons: ^1.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
